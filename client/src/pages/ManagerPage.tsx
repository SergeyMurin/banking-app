import * as React from "react";

interface IManagerPageProps {}

const ManagerPage: React.FunctionComponent<IManagerPageProps> = (props) => {
  return <div>Manager Page</div>;
};

export default ManagerPage;
