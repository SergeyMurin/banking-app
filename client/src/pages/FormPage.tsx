import * as React from "react";
import Form from "../components/form/Form";

interface IFormPageProps {}

const FormPage: React.FunctionComponent<IFormPageProps> = (props) => {
  return (
    <div>
      <Form/>
    </div>
  )
};

export default FormPage;
